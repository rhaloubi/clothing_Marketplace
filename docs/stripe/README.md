# Stripe-inspired dashboard

- **Design system (canonical):** [DESIGN.md](./DESIGN.md) — colors, typography intent, shadows, radius, component recipes.
- **Implementation:** `src/styles/globals.css` (`stripe-*` theme + `shadow-stripe-card`), `src/components/dashboard/dashboard-page.tsx`, and shell components under `src/components/dashboard/shell/`.
